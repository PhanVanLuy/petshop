import {environment} from '../../environments/environment';

export class ApiConstants {
    public static REFUND_MESSAGE = environment.serverUrl + 'xxx';
    public static CONTACT_MESSAGE = environment.serverUrl + 'xxx';
    public static CHECK_COUPON = environment.serverUrl + 'xxx';
    public static ORDER = environment.serverUrl + 'xxx';
    public static COUNTRY_CODE = 'xxx';
}
